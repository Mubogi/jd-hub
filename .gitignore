# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv/

# Django
db.sqlite3
db.sqlite3-journal
staticfiles/
# Keep the directory but not generated PDFs (they're rebuilt on deploy/first access)
media/system_docs/*.pdf
media/lesson_docs/*.pdf
# Allow committed gallery + screenshot images (needed on deploy).
# Only generated PDFs above are ignored.

# Environment / secrets
.env
.env.*
*.pem
*.key

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
# Google service account keys — never commit
*-96093db76cde.json
*-iam.gserviceaccount*.json
service-account*.json
